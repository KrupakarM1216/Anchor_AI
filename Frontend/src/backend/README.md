# Backend

Server-only code belongs here: request validation, deterministic engines, curated evidence repositories, safety processing, and eventual AI orchestration. Next.js route handlers in `src/app/api` should remain thin adapters.
