# ANCHOR Frontend

The Next.js frontend application. Run `npm run dev` from this folder. Browser UI code is in `src/frontend`; route pages are in `src/app`.

The separate API workspace is `../Backend`.
