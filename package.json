{
  "name": "anchor-ai",
  "private": true,
  "workspaces": [
    "Frontend",
    "Backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=anchor-web",
    "dev:frontend": "npm run dev --workspace=anchor-web",
    "dev:backend": "npm run dev --workspace=anchor-backend",
    "build": "npm run build --workspace=anchor-web",
    "lint": "npm run lint --workspace=anchor-web"
  }
}
