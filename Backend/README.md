# ANCHOR Backend

Standalone server workspace for ANCHOR APIs, deterministic engines, curated evidence, safety processing, and optional server-only AI orchestration.

Run `npm run dev` to start the development server on port 4000. `GET /health` is available now. The Next.js prototype remains in `../Frontend`; its current route adapters will be migrated into this service as the backend is expanded.
